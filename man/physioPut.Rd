% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provenance-bridge.R
\name{physioPut}
\alias{physioPut}
\title{Store a Physio object in a lake with its provenance linked in the lineage}
\usage{
physioPut(lake, name, x, tags = "physio", provenance_suffix = "__prov")
}
\arguments{
\item{lake}{An OmicsLake \code{Lake}.}

\item{name}{Object name.}

\item{x}{A Physio object, e.g. a \link[PhysioCore:PhysioExperiment]{PhysioCore::PhysioExperiment}.}

\item{tags}{Character tags for the object (default \code{"physio"}).}

\item{provenance_suffix}{Suffix for the companion provenance table
(default \code{"__prov"}).}
}
\value{
Invisibly, the provenance table's name, or \code{NA_character_} if the
object carried no provenance.
}
\description{
Wraps \link[OmicsLake:Lake]{OmicsLake::Lake}'s \code{put()} so that the object's W3C-PROV operation DAG
(from \code{\link[PhysioCore:provenance]{PhysioCore::provenance()}}) is stored as a queryable companion table and
recorded as a \strong{lineage dependency} of the object (via \code{put(..., depends_on =)}). The ecosystem's per-object (micro) provenance thus becomes a first-class
node in OmicsLake's cross-dataset (macro) lineage, visible to \code{lake$tree()},
\code{lake$deps()}, and \code{lake$impact()}.
}
\details{
Objects that carry no provenance are stored normally (no companion table).
}
\seealso{
\code{\link[=physioProvenance]{physioProvenance()}}
}
